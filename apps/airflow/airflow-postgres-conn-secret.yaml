apiVersion: v1
kind: Secret
metadata:
  name: airflow-pg-conn-secret # Name referenced in airflow-values.yaml
  namespace: airflow
type: Opaque
stringData: # Use stringData for plain text values
  # The key name for metadataSecretName is 'connection'
  connection: "postgresql://airflow:Lazarus17@postgresql.postgres.svc.cluster.local:5432/airflow"